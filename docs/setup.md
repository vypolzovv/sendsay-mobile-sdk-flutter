---
title: Initial SDK setup
excerpt: Install and configure the Flutter SDK
slug: flutter-sdk-setup
categorySlug: integrations
parentDocSlug: flutter-sdk
---
## Установка SDK

Flutter SDK Sendsay можно установить или обновить с помощью зависимости в файле [pubspec](https://docs.flutter.dev/tools/pubspec) вашего приложения. Для настройки iOS-приложения требуется [CocoaPods](https://cocoapods.org/).

> 📘
>
> Актуальную версию Flutter SDK Sendsay можно найти:
> - в репозитории GitHub — https://github.com/sendsay-ru/sendsay-mobile-sdk-flutter/tags 
> - на pub.dev — https://pub.dev/packages/sendsay.

### Добавление зависимости

В файле `pubspec.yaml` вашего проекта добавьте зависимость от Flutter SDK Sendsay в раздел `dependencies:`:

```yaml
dependencies:
  sendsay: ^0.1.0
```

### Настройка iOS

Для разрешения зависимостей SDK на iOS перейдите в каталог `ios`:

```shell
cd ios
```

Выполните команду:

```shell
pod install
```

Минимальная поддерживаемая версия iOS для Sendsay SDK — **13.0**. 

При необходимости измените первую строку в файле `ios/Podfile`:

```
platform :ios, '13.0'
```

### Настройка Android

Минимальный поддерживаемый уровень API Android для Sendsay SDK — **24**. 

При необходимости обновите значение `minSdk` в файле `android/app/build.gradle` до `24` или выше:

```gradle
android {
    ...
    defaultConfig {
        ...
        minSdk 24
    }
}
```

## Инициализируйте SDK.

После установки SDK необходимо импортировать, настроить и инициализировать его в коде приложения.

Для инициализации требуются следующие параметры конфигурации:
- `projectToken`, 
- `authorizationToken`,
- `baseURL`. 

Эти значения можно найти в веб-приложении [Sendsay](https://app.sendsay.ru/dashboard), в разделе **Подписчики** > **Мобильное приложение** > **Настройки приложения и импорта** > **Работа через SDK**.

> 📘
>
> Подробнее — в разделе [Управление доступом к мобильным SDK](https://docs.sendsay.ru/sendsay-api/how-to-get-apikey).

Импорт SDK:

```dart
import 'package:sendsay/sendsay.dart';
```

Инициализация:

```dart
final _plugin = SendsayPlugin();
final configuration = SendsayConfiguration(
  projectToken: 'YOUR_PROJECT_TOKEN',
  authorizationToken: 'YOUR_API_KEY',
  // значение baseUrl по умолчанию это https://mobi.sendsay.ru/xnpe/v100
  baseUrl: 'YOUR_API_BASE_URL', 
);
_plugin.configure(configuration).catchError((error) {
  print('Error: $error');
  return false;
});
```

### Настройте SDK только один раз

Flutter-приложение может перезагружаться (hot reload) без перезапуска нативной части. При этом нативный код SDK продолжает работать.

Поэтому SDK необходимо настроить **только один раз**. При разработке с включённой функцией hot reload, перед настройкой SDK необходимо проверить `SendsayPlugin().isConfigured()`.

```dart
Future<void> configureSendsay(SendsayConfiguration configuration) {
  try {
    if (!await _plugin.isConfigured()) {
      _plugin.configure(configuration);
    } else {
      print("Sendsay SDK already configured.");
    }
  } catch (error) {
    print('Error: $error');
  }
}
```

### Инициализация завершена

На этом этапе SDK активен и начинает автоматически отслеживать сессии в вашем приложении.

## Другие настройки SDK

### Расширенная конфигурация

SDK можно дополнительно настроить, указав свойства в объекте `SendsayConfiguration`. 

Полный список доступных параметров смотрите в разделе [Настройка](../docs/configuration.md).

### Уровень Логирования

SDK поддерживает следующие уровни логирования, определённые в `LogLevel`:

| Уровень логирования | Описание |
|--------|----------|
| `off` | Отключает всё логирование |
| `error` | Серьёзные ошибки или критические проблемы |
| `warning` | Предупреждения и рекомендации + `error` |
| `info` | Информационные сообщения + `warning` + `error` |
| `debug` | Отладочная информация + `info` + `warning` + `error` |
| `verbose` | Полный лог всех действий SDK Информация обо всех действиях SDK + `debug` + `info` + `warning` + `error`|


По умолчанию используется уровень `info`. При разработке или отладке может быть полезно установить уровень `debug` или `verbose`.

Чтобы изменить уровень:

```dart
_plugin.setLogLevel(LogLevel.verbose);
```

### Выгрузка данных

Подробнее о том, как SDK отправляет данные в Sendsay API и как управлять этим поведением, смотрите в разделе [Выгрузка данных](../docs/data-flushing.md).
