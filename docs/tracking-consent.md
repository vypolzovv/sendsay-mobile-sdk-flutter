---
title: Tracking Consent
excerpt: Manage tracking consent using the Flutter SDK.
slug: flutter-sdk-tracking-consent
categorySlug: integrations
parentDocSlug: flutter-sdk-tracking
---

В зависимости от локальных правил доступа к данным, для доступа к данным на устройстве пользователя может потребоваться явное согласие. Чтобы соблюдать такие требования, CDP Sendsay позволяет включить автономную функцию «согласие на отслеживание». Эта функция активирует опцию согласия на отслеживание для сообщений в приложении, блоков контента в приложении и мобильных push-уведомлений.

## Как SDK управляет согласием на отслеживание
Вот перевод на русский. Команды, методы и параметры оставлены в оригинале 👇

⸻

Если функция tracking consent включена, платформа Sendsay отправляет атрибут has_tracking_consent вместе с push-уведомлениями, in-app сообщениями и данными in-app content blocks. SDK трекает события в зависимости от булевого значения этого атрибута.

Если функция tracking consent отключена, атрибут has_tracking_consent не включается в push-уведомления, in-app сообщения и данные in-app content blocks. В этом случае SDK считает has_tracking_consent равным true и трекает события соответственно.

Для событий клика есть возможность переопределить значение has_tracking_consent и принудительно включить трекинг, добавив query-параметр xnpe_force_track со значением true в action URL.

Как SDK трекает события в зависимости от tracking consent

## Push Notification Delivered

SDK трекает доставку push-уведомления, вызывая SendsayPlugin().trackDeliveredPush или SendsayPlugin.handleRemoteMessage. Эти методы трекают событие доставки только если выполняется одно из условий:
•	Функция tracking consent отключена.
•	Функция tracking consent включена и has_tracking_consent равен true.

Если вы вызываете метод SendsayPlugin().trackDeliveredPush вручную и хотите игнорировать tracking consent, можно использовать SendsayPlugin().trackDeliveredPushWithoutTrackingConsent. Этот метод будет трекать событие независимо от tracking consent.

## Push Notification Clicked

SDK трекает клики по push-уведомлениям, вызывая SendsayPlugin().trackClickedPush. Этот метод трекает событие клика только если выполняется одно из условий:
•	Функция tracking consent отключена.
•	Функция tracking consent включена и has_tracking_consent равен true.
•	Action URL содержит query-параметр xnpe_force_track со значением true (переопределяет has_tracking_consent).

👍

Событие, которое было трекнуто из-за включённого xnpe_force_track (принудительный трекинг), будет содержать дополнительное свойство tracking_forced со значением true.

Если вы вызываете метод SendsayPlugin().trackClickedPush вручную и хотите игнорировать tracking consent, можно использовать SendsayPlugin().trackPushOpenedWithoutTrackingConsent.

Эти методы трекают событие независимо от tracking consent.

## In-App Message Clicked

SDK трекает клики по in-app сообщениям, вызывая `SendsayPlugin().trackInAppMessageClick`. Этот метод трекает событие клика только если выполняется одно из условий:
•	Функция tracking consent отключена.
•	Функция tracking consent включена и has_tracking_consent равен true.
•	Action URL содержит query-параметр xnpe_force_track со значением true (переопределяет has_tracking_consent).

👍

Событие, которое было трекнуто из-за включённого xnpe_force_track (принудительный трекинг), будет содержать дополнительное свойство tracking_forced со значением true.

Если вы вызываете метод SendsayPlugin().trackInAppMessageClick вручную и хотите игнорировать tracking consent, можно использовать SendsayPlugin().trackInAppMessageClickWithoutTrackingConsent. Этот метод будет трекать событие независимо от tracking consent.

## In-App Message Closed

SDK трекает события закрытия in-app сообщений, вызывая SendsayPlugin().trackInAppMessageClose. Этот метод трекает событие закрытия только если выполняется одно из условий:
•	Функция tracking consent отключена.
•	Функция tracking consent включена и has_tracking_consent равен true.

Если вы вызываете метод SendsayPlugin().trackInAppMessageClose вручную и хотите игнорировать tracking consent, можно использовать SendsayPlugin().trackInAppMessageCloseWithoutTrackingConsent. Этот метод будет трекать событие независимо от tracking consent.

## App Inbox Message Opened

SDK трекает открытие сообщений в app inbox, вызывая SendsayPlugin().trackAppInboxOpened. Этот метод трекает событие открытия сообщения только если выполняется одно из условий:
•	Функция tracking consent отключена.
•	Функция tracking consent включена и has_tracking_consent равен true.
•	App Inbox загружен, и переданный MessageItem присутствует в App Inbox.

Если вы вызываете метод SendsayPlugin().trackAppInboxOpened вручную и хотите игнорировать tracking consent, можно использовать SendsayPlugin().trackAppInboxOpenedWithoutTrackingConsent. Этот метод будет трекать событие независимо от tracking consent.

## App Inbox Action Clicked

SDK трекает клики по действиям в app inbox, вызывая SendsayPlugin().trackAppInboxClick. Этот метод трекает событие клика только если выполняется одно из условий:
•	Функция tracking consent отключена.
•	Функция tracking consent включена и has_tracking_consent равен true.
•	Action URL содержит query-параметр xnpe_force_track со значением true (переопределяет has_tracking_consent).

👍

Событие, которое было трекнуто из-за включённого xnpe_force_track (принудительный трекинг), будет содержать дополнительное свойство tracking_forced со значением true.

Если вы вызываете метод SendsayPlugin().trackAppInboxClick вручную и хотите игнорировать tracking consent, можно использовать SendsayPlugin().trackAppInboxClickWithoutTrackingConsent. Этот метод будет трекать событие независимо от tracking consent.

## In-App Content Block Displayed

SDK трекает показ in-app content block, вызывая SendsayPlugin().trackInAppContentBlockShown. Этот метод трекает событие показа только если выполняется одно из условий:
•	Функция tracking consent отключена.
•	Функция tracking consent включена и has_tracking_consent равен true.

Если вы вызываете метод SendsayPlugin().trackInAppContentBlockShown вручную и хотите игнорировать tracking consent, можно использовать SendsayPlugin().trackInAppContentBlockShownWithoutTrackingConsent. Этот метод будет трекать событие независимо от tracking consent.

## In-App Content Block Clicked

SDK трекает клики по in-app content block, вызывая SendsayPlugin().trackInAppContentBlockClick. Этот метод трекает событие клика только если выполняется одно из условий:
•	Функция tracking consent отключена.
•	Функция tracking consent включена и has_tracking_consent равен true.
•	Action URL содержит query-параметр xnpe_force_track со значением true (переопределяет has_tracking_consent).

👍

Событие, которое было трекнуто из-за включённого xnpe_force_track (принудительный трекинг), будет содержать дополнительное свойство tracking_forced со значением true.

Если вы вызываете метод SendsayPlugin().trackInAppContentBlockClick вручную и хотите игнорировать tracking consent, можно использовать SendsayPlugin().trackInAppContentBlockClickWithoutTrackingConsent. Этот метод будет трекать событие независимо от tracking consent.

## In-App Content Block Closed

SDK трекает события закрытия in-app content block, вызывая SendsayPlugin().trackInAppContentBlockClose. Этот метод трекает событие закрытия только если выполняется одно из условий:
•	Функция tracking consent отключена.
•	Функция tracking consent включена и has_tracking_consent равен true.

Если вы вызываете метод SendsayPlugin().trackInAppContentBlockClose вручную и хотите игнорировать tracking consent, можно использовать SendsayPlugin().trackInAppContentBlockCloseWithoutTrackingConsent. Этот метод будет трекать событие независимо от tracking consent.

## In-App Content Block Error

SDK трекает ошибки in-app content block, вызывая SendsayPlugin().trackInAppContentBlockError с осмысленным параметром errorMessage. Этот метод трекает событие только если выполняется одно из условий:
•	Функция tracking consent отключена.
•	Функция tracking consent включена и has_tracking_consent равен true.

Если вы вызываете метод SendsayPlugin().trackInAppContentBlockError вручную и хотите игнорировать tracking consent, можно использовать SendsayPlugin().trackInAppContentBlockErrorWithoutTrackingConsent. Этот метод будет трекать событие независимо от tracking consent.
