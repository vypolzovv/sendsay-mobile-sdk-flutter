---
title: Android push notifications
excerpt: Enable push notifications on Android using the Flutter SDK
slug: flutter-sdk-push-android
categorySlug: integrations
parentDocSlug: flutter-sdk-push-notifications
---

Flutter SDK использует [нативный Android SDK](https://github.com/sendsay-ru/sendsay-mobile-sdk-android) для обработки push-уведомлений на устройствах Android. В этом разделе описаны основные шаги настройки push-уведомлений в контексте Flutter SDK. 

Подробные инструкции и расширенные настройки смотрите в [документации по push-уведомлениям для Android SDK](https://github.com/sendsay-ru/sendsay-mobile-sdk-android/blob/main/docs/push-notifications.md).

> ❗️
>
> На доставку push-уведомлений и отслеживание кликов может влиять функция согласия на отслеживание. Если она включена, для трекинга требуется явное согласие пользователя. Подробнее — в разделе [Согласие на отслеживание](tracking-consent.md).

## Интеграция push-уведомлений

Android SDK поддерживает следующие варианты интеграции push-уведомлений:

- [Стандартная интеграция (Firebase)](#стандартная-firebase-интеграция)
- [Интеграция с Huawei](#интеграция-с-huawei)

### Стандартная интеграция (Firebase)

Чтобы отправлять [push-уведомления](push-notifications.md) из CDP Sendsay и получать их в Android-приложении, необходимо:

1. Настроить проект Firebase.
2. Реализовать Firebase messaging в приложении.
3. Подключить интеграцию **Firebase Cloud Messaging** (FCM) в настройках мобильного приложения в личном кабинете CDP Sendsay.

> 📘
>
> Следуйте инструкциям в разделе [Firebase Cloud Messaging](https://docs.sendsay.ru/other-channels/mobile-push/how-to-connect-mobile-push#%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D1%8B-%D0%BF%D0%BE%D0%B4%D0%BA%D0%BB%D1%8E%D1%87%D0%B5%D0%BD%D0%B8%D1%8F) документации по нативному Android SDK.
>
> Обратите внимание: корнем Android-проекта Flutter является каталог `/android`.

> 👍
>
> В связи с прекращением поддержки устаревшего API FCM в июне 2024 года CDP Sendsay использует **Firebase HTTP v1 API**. Подробнее — в разделе [Обновление Firebase до HTTP v1 API](https://support.bloomreach.com/hc/en-us/articles/18931691055133-Firebase-upgrade-to-HTTP-v1-API) в справочном центре поддержки.

### Интеграция с Huawei

Чтобы отправлять push-уведомления на устройства Huawei, необходимо:

1. Настроить Huawei Mobile Services (HMS).
2. Подключить HMS в приложении.
3. Настроить интеграцию Huawei Push Service в настройках мобильного приложения в личном кабинете CDP Sendsay.

> 📘
>
> Следуйте инструкциям в разделе [Huawei Mobile Services](https://docs.sendsay.ru/other-channels/mobile-push/how-to-connect-mobile-push#%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D1%8B-%D0%BF%D0%BE%D0%B4%D0%BA%D0%BB%D1%8E%D1%87%D0%B5%D0%BD%D0%B8%D1%8F) в документации по нативному Android SDK.

### Запрос разрешения на push-уведомления

Начиная с Android 13 (API 33), для отображения push-уведомлений требуется разрешение `POST_NOTIFICATIONS`, которое должно быть предоставлено пользователем во время выполнения.

SDK автоматически добавляет это разрешение в `AndroidManifest.xml`, однако диалог запроса разрешения должен быть вызван из приложения.

Для этого можно использовать API SDK:

```dart 
_plugin.requestPushAuthorization()
.then((accepted) => print("User has ${accepted ? 'accepted': 'rejected'} push notifications."))
.catchError((error) => print('Error: $error'));
```

Логика поведения вызова:

* Android API < 33:
  * Разрешение не требуется, всегда возвращается `true`.
* Android API 33+:
  * Если разрешение уже выдано > возвращает `true`, без показа диалога.
  * Если нет > показывает системный диалог и возвращает решение пользователя.

## Настройка

### Настройка deep-links

Для [обработки переходов по ссылкам](push-notifications.md#реагирование-на-взаимодействия-с-push-уведомлениями) из push-уведомлений используйте `SendsayPlugin().openedPushStream`.

Чтобы deep links корректно работали на Android, необходимо внести изменения в `AndroidManifest`.

#### Установите режим запуска активности в режиме одной задачи

По умолчанию Android может создавать новую activity при открытии ссылки. Чтобы этого избежать, установите режим запуска `android:launchMode=«singleTask»` для основной активности:

```xml
<activity
  android:name=".MainActivity"
  ...
  android:launchMode="singleTask"
>
```

### Настройка intent-фильтра

Также необходимо добавить intent-фильтр, который будет обрабатывать ссылки из push-уведомлений. Можно использовать пользовательскую схему или URL-адрес. 

Подробнее — в официальной [документации Android](https://developer.android.com/training/app-links/deep-linking#adding-filters).

```xml
<activity ...>
   ...
  <intent-filter>
    <action android:name="android.intent.action.VIEW" />
    <category android:name="android.intent.category.DEFAULT" />
    <category android:name="android.intent.category.BROWSABLE" />

      <!-- Accepts URIs that begin with "sendsay://action”-->
      <data android:scheme="sendsay" android:host="action" />

      <!-- Accepts URIs that begin https://www.example.com -->
      <data android:scheme="https" android:host="www.example.com" />
  </intent-filter>
</activity>
```


### Отображение уведомления в приложении

По умолчанию, если push-уведомление получено, когда приложение находится в активном состоянии, уведомление отображается в панели уведомлений, а значок появляется в строке состояния.

Чтобы показывать собственные баннеры уведомлений, можно настроить [слушатель для полученных push-уведомлений](push-notifications.md#реакция-на-полученние-push-уведомления) с помощью `SendsayPlugin.receivedPushStream` и реализовать отображение баннера самостоятельно, например, с помощью пакета [flutter_local_notifications](https://pub.dev/packages/flutter_local_notifications).

```dart
final subscription = _plugin.receivedPushStream.listen((receivedPush) {
  _showPushNotification(receivedPush);
});
```

Когда слушатель больше не нужен, вызовите:

```dart
subscription.cancel()
```

> ❗️
>
> На Android слушатель вызывается как для обычных, так и для тихих push-уведомлений. На iOS — **только** для тихих push-уведомлений из-за ограничений платформы.

## Устранение неполадок

### Приложение не открывается по нажатию на push-уведомление (Xiaomi / Redmi)

На устройствах с MIUI настройки оптимизации работы батареи могут влиять на работу push-уведомлений.

Рекомендуется:

- отключить оптимизацию батареи в разделе **Настройки** > **Батарея и производительность**;
- установить режим «Без ограничений» в настройках экономии батареи для приложения;
- отключить оптимизацию MIUI в **настройках разработчика**.

### Отсутствует push-токен после вызова anonymize

Приложение может использовать `SendsayPlugin().anonymize()` в качестве функции выхода из системы.

Вызов метода `anonymize` удалит токен push-уведомлений из локального хранилища. После анонимизации необходимо заново получить push-токен и передать его в SDK — сразу после anonymize или после последующей идентификации клиента.

```kotlin
import android.os.Bundle
import ru.sendsay.SendsayPlugin
import io.flutter.embedding.android.FlutterActivity

class MainActivity : FlutterActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        FirebaseMessaging.getInstance().token.addOnSuccessListener {
            SendsayPlugin.handleNewGmsToken(applicationContext, it)
        }
    }
}
```
