---
title: Android push notifications
excerpt: Enable push notifications on Android using the Flutter SDK
slug: flutter-sdk-push-android
categorySlug: integrations
parentDocSlug: flutter-sdk-push-notifications
---

SDK Flutter использует [нативный SDK Android](https://github.com/sendsay-ru/sendsay-mobile-sdk-android) для обработки push-уведомлений в Android. В этом руководстве приведены сокращенные инструкции для Android в контексте Flutter SDK, а подробная информация приведена в [документации по push-уведомлениям для Android SDK](https://github.com/sendsay-ru/sendsay-mobile-sdk-android/blob/main/docs/push-notifications.md).

> ❗️
>
> На работу доставки push-уведомлений и отслеживания кликов может повлиять функция согласия на отслеживание, которая, если включена, требует явного согласия на отслеживание. Подробности см. в [документации по согласию на отслеживание](tracking-consent.md).

## Интеграция

Sendsay Android SDK поддерживает следующие интеграции:

- [Стандартная (Firebase) интеграция](#стандартная-firebase-интеграция)
- [Интеграция с Huawei](#интеграция-с-huawei)

### Стандартная (Firebase) интеграция

Чтобы иметь возможность отправлять [push-уведомления](push-notifications.md) с платформы CDP Sendsay и получать их в своем приложении на устройствах Android, необходимо:

1. Настроить проект Firebase.
2. Реализовать обмен сообщениями Firebase в своем приложении.
3. Настроить интеграцию Firebase Cloud Messaging в веб-приложении CDP Sendsay.

> 📘
>
> Следуйте инструкциям в разделе [Firebase Cloud Messaging](https://docs.sendsay.ru/other-channels/mobile-push/how-to-connect-mobile-push#%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D1%8B-%D0%BF%D0%BE%D0%B4%D0%BA%D0%BB%D1%8E%D1%87%D0%B5%D0%BD%D0%B8%D1%8F) в документации по нативному SDK для Android.
>
> Обратите внимание, что корнем вашего проекта Android является `/android`.

> 👍
>
> Обратите внимание, что в связи с тем, что Google прекращает поддержку и удаляет устаревший API FCM в июне 2024 года, CDP Sendsay теперь использует API Firebase HTTP v1. Информацию об обновлении см. в разделе [Обновление Firebase до HTTP v1 API](https://support.bloomreach.com/hc/en-us/articles/18931691055133-Firebase-upgrade-to-HTTP-v1-API) в справочном центре поддержки.

### Интеграция с Huawei

Чтобы иметь возможность отправлять [push-уведомления](push-notifications.md) с платформы CDP Sendsay и получать их в своем приложении на устройствах Huawei, необходимо:

1. Настроить Huawei Mobile Services (HMS).
2. Внедрить HMS в свое приложение.
3. Настроить интеграцию Huawei Push Service в веб-приложении CDP Sendsay.

> 📘
>
> Следуйте инструкциям в разделе [Huawei Mobile Services](https://docs.sendsay.ru/other-channels/mobile-push/how-to-connect-mobile-push#%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D1%8B-%D0%BF%D0%BE%D0%B4%D0%BA%D0%BB%D1%8E%D1%87%D0%B5%D0%BD%D0%B8%D1%8F) в документации по нативному Android SDK.

### Запрос разрешения на уведомления

Начиная с Android 13 (уровень API 33), новое разрешение на уведомления во время выполнения `POST_NOTIFICATIONS` должно быть зарегистрировано в вашем `AndroidManifest.xml`, а также должно быть предоставлено пользователем, чтобы ваше приложение могло отображать push-уведомления.

SDK уже регистрирует разрешение `POST_NOTIFICATIONS`.

Диалоговое окно разрешения на выполнение, в котором пользователю предлагается предоставить разрешение, должно запускаться из вашего приложения. Для этой цели вы можете использовать API SDK:

```dart 
_plugin.requestPushAuthorization()
.then((accepted) => print("User has ${accepted ? 'accepted': 'rejected'} push notifications."))
.catchError((error) => print('Error: $error'));
```

Поведение этого обратного вызова следующее:

* Для Android API уровня <33:
  * Разрешение не требуется, автоматически возвращается `true`.
* Для Android API уровня 33+:
  * Отобразить диалоговое окно, вернуть решение пользователя (`true`/`false`).
  * В случае ранее предоставленного разрешения не отображать диалоговое окно, вернуть `true`.

## Настройка

### Включение deep-links

Вы можете использовать `SendsayPlugin().openedPushStream` для определения [слушателя, который будет реагировать на взаимодействия с push-уведомлениями](push-notifications.md#реагирование-на-взаимодействия-с-push-уведомлениями). Чтобы включить deep-links, необходимо внести некоторые изменения в `AndroidManifest` в `android/src/main`.

#### Установите режим запуска активности в режиме одной задачи

По умолчанию Android запускает новую activity для вашего приложения, когда пользователь открывает deep-link. Вы должны переопределить это поведение, установив `android:launchMode=«singleTask»` для вашей основной активности:

```xml
<activity
  android:name=".MainActivity"
  ...
  android:launchMode="singleTask"
>
```

### Определите intent фильтр

Вы также должны определить intent фильтр, который может реагировать на ссылку push-уведомления. Вы можете использовать либо настраиваемую схему, либо URL-адрес. Дополнительную информацию см. в соответствующей официальной [документации Android](https://developer.android.com/training/app-links/deep-linking#adding-filters).

```xml
<activity ...>
   ...
  <intent-filter>
    <action android:name="android.intent.action.VIEW" />
    <category android:name="android.intent.category.DEFAULT" />
    <category android:name="android.intent.category.BROWSABLE" />

      <!-- Accepts URIs that begin with "sendsay://action”-->
      <data android:scheme="sendsay" android:host="action" />

      <!-- Accepts URIs that begin https://www.example.com -->
      <data android:scheme="https" android:host="www.example.com" />
  </intent-filter>
</activity>
```


### Показ уведомления в приложении

По умолчанию, если уведомление поступает, когда приложение в открытом состоянии, оно попадает в панель уведомлений, а значок уведомления появляется в строке состояния.

Чтобы включить баннеры уведомлений, когда приложение в открытом состоянии, можно настроить [слушатель для полученных push-уведомлений](push-notifications.md#реакция-на-полученние-push-уведомления) с помощью `SendsayPlugin.receivedPushStream` и реализовать отображение баннера уведомления с помощью стороннего плагина, такого как [flutter_local_notifications](https://pub.dev/packages/flutter_local_notifications).

```dart
final subscription = _plugin.receivedPushStream.listen((receivedPush) {
  _showPushNotification(receivedPush);
});
```

Вызовите `subscription.cancel()`, когда вам больше не нужен слушатель.

> ❗️
>
> Слушатель вызывается как для обычных, так и для тихих push-уведомлений на Android, но **только** для тихих push-уведомлений на iOS из-за технических ограничений.

## Устранение неполадок

Если push-уведомления в вашем приложении работают не так, как ожидалось, обратите внимание на следующие частые проблемы и их возможные решения:

### При нажатии на push-уведомление приложение не открывается на устройствах Xiaomi / Redmi.

Xiaomi MIUI по-своему оптимизирует работу батареи, что иногда может повлиять на работу push-уведомлений.

Если на устройствах с MIUI включена оптимизация батареи, push-уведомления могут перестать отображаться или не работать после нажатия. К сожалению, мы не можем ничего сделать, чтобы предотвратить это, но вы можете попробовать следующее, чтобы решить проблему:

- Отключите все настройки оптимизации батареи в «Настройках» > «Батарея и производительность».
- Установите опцию «Без ограничений» в настройках экономии батареи для вашего приложения.
- И (возможно) самое важное: отключите «Оптимизация памяти и MIUI» в «Настройках для разработчиков».

### После анонимизации отсутствует токен push-уведомления

Ваше приложение может использовать `SendsayPlugin().anonymize()` в качестве функции выхода из системы.

Имейте в виду, что вызов метода `anonymize` удалит токен push-уведомлений из хранилища. Ваше приложение должно вручную получить действительный токен перед использованием любых функций push-уведомлений. Вы можете сделать это сразу после `anonymize` или до или после `identifyCustomer`, в зависимости от сценариев push-уведомлений.

```kotlin
import android.os.Bundle
import ru.sendsay.SendsayPlugin
import io.flutter.embedding.android.FlutterActivity

class MainActivity : FlutterActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        FirebaseMessaging.getInstance().token.addOnSuccessListener {
            SendsayPlugin.handleNewGmsToken(applicationContext, it)
        }
    }
}
```
