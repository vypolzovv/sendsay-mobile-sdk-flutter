---
title: Tracking
excerpt: Track customers and events using the Flutter SDK
slug: flutter-sdk-tracking
categorySlug: integrations
parentDocSlug: flutter-sdk
---

Вы можете отслеживать события в CDP Sendsay, чтобы анализировать использование приложения и сегментировать клиентов по их действиям.

По умолчанию SDK автоматически отслеживает ряд базовых событий, включая:

* установку приложения (после первой установки и после вызова [anonymize](#anonymize));
* начало и окончание пользовательской сессии;
* событие показа баннера для in-app сообщений и блоков контента.

Кроме того, SDK позволяет отслеживать любые пользовательские события, которые имеют значение для вашего бизнеса.

> 👍
>
> Все примеры на этой странице предполагают, что `SendsayPlugin` доступен как `_plugin`. 
> Подробнее — в разделе документации [Инициализация SDK](https://docs.sendsay.ru/sources-and-events/mobile-app/#%D0%B8%D0%BD%D1%82%D0%B5%D0%B3%D1%80%D0%B0%D1%86%D0%B8%D1%8F-%D1%81-%D0%BF%D1%80%D0%B8%D0%BB%D0%BE%D0%B6%D0%B5%D0%BD%D0%B8%D0%B5%D0%BC-%D1%87%D0%B5%D1%80%D0%B5%D0%B7-sdk).

## События

### Отслеживание событий

Для отслеживания пользовательских событий используйте метод `trackEvent()`, передав в него объект `Event`.

Тип события может иметь любое имя. Рекомендуется использовать понятные и описательные названия, отражающие суть действия.

Обзор часто используемых пользовательских событий смотрите в документации [Пользовательские события](https://sendsay.ru/api/api.html#%D0%A1%D0%BE%D0%B1%D1%8B%D1%82%D0%B8%D1%8F).

#### Аргументы

| Имя  | Тип               | Описание |
| ----- |-------------------| ----------- |
| event | [Event](#Событие) | Объект события. |

##### Событие

| Имя                | Тип                 | Описание |
| --------------------| -------------------- | ----------- |
| name **(обязательно)** | Строка               | Имя типа события, например, `screen_view`. |
| properties         | Map<String, dynamic> | Словарь свойств события. |
| timestamp           | DateTime?            | Временная метка события (Unix time, в секундах). По умолчанию используется текущее время. |

#### Примеры

##### Пример 1. Отслеживание просмотра экрана

Предположим, вам нужно понимать, какие экраны приложения чаще всего просматривают пользователи. Для этого можно отправлять событие `screen_view`.

Сначала создайте `Event` с именем и свойствами, которые вы хотите отслеживать с помощью этого события. Чтобы отслеживать название экрана, включите свойство `screen_name` вместе с другими соответствующими свойствами:

```dart
final event = Event(
  name: 'screen_view',
  properties: {
    'screen_name': "dashboard",
    'other_property': 123.45,
  },
);
```

Передайте объект события в `trackEvent()` следующим образом:

```dart
_plugin.trackEvent(event);
```

##### Пример 2. События со сложными свойствами

```dart
final event = Event(
  name: 'purchase',
  properties: {
    'purchase_status': "success",
    'product_list': [
        {'product_id': 'abc123', 'quantity': 2},
        {'product_id': 'abc456', 'quantity': 1}
    ],
    'total_price': 7.99,
  },
);
_plugin.trackEvent(event);
```

> 👍
>
> При необходимости можно указать собственный `timestamp`, если событие произошло ранее.

#### SSEC (События модуля «Продажи»)

Подробное описание доступно в документации: [События модуля «Продажи»](https://docs.sendsay.ru/ecom/how-to-configure-data-transfer).

```dart
final DateFormat formatter = DateFormat("yyyy-MM-dd HH:mm:ss");
final String currentDateTime = formatter.format(DateTime.now());

final event = SSECEvent(
        type: TrackingSSECType.viewProduct,
        data: TrackSSEC.viewProduct()
            .product(
                id: "123456",
                datetime: currentDateTime,
                name: "Notebook",
                price: 1.0,
            ).buildData(),
    );
_plugin.trackSSECEvent(event.toSsecMap())
```

#### CCE (Пользовательские события) [в разработке]

```dart
    final Map properties = {};
    final customJsonString = """{
            'property': '5s',
            'int': 12,
            'double': 34.56,
            'string': 'test'
        }""".trim();
    const eventType = "custom_event_name";

    properties["cce"] = customJsonString;

    // Отправка события
    const event = Event(
      name: eventType,
      properties: properties,
    );
    await _plugin.trackEvent(event);
```

## Клиенты

Идентификация клиентов позволяет отслеживать их действия на разных устройствах и платформах.

Без идентификации SDK связывает события с анонимным клиентом через автоматически сгенерированный cookie.

После идентификации клиента (по email, телефону, csid и т. д.) события будут связаны с этим идентификатором.

> 👍
>
> Пользователь приложения и профиль клиента — разные сущности с собственными жизненными циклами. Учитывайте это при использовании методов [identify](#идентификация) и [anonymize](#anonymize).

### Идентификация

Для идентификации клиента используйте метод `identifyCustomer()`, передав объект `Customer`.

По умолчанию жёстким идентификатором является `registered` (обычно email), но в проекте может быть настроен другой идентификатор. При желании вы можете отслеживать дополнительные свойства клиента, такие как имя и фамилия, возраст и т. д.

#### Аргументы

| Имя                     | Тип               | Описание |
| ------------------------ |-------------------| ----------- |
| customer **(обязательно)**  | [Клиент](#Клиент) | Объект клиента. |

##### Клиент

| Имя | Тип | Описание |
| --- | --- | --- |
| ids **(обязательно)** | Map<String, String> | Словарь уникальных идентификаторов клиента. Принимаются только идентификаторы, определённые в проекте CDP Sendsay. |
| properties | Map<String, dynamic> | Словарь свойств клиента. |

#### Передача данных в профиль клиента (member.set)

Чтобы узнать о передаче данных в профиль клиента, обратитесь к описанию [метода member.set](https://sendsay.ru/api/api.html#%D0%A1%D0%BE%D0%B7%D0%B4%D0%B0%D1%82%D1%8C-%D0%BF%D0%BE%D0%B4%D0%BF%D0%B8%D1%81%D1%87%D0%B8%D0%BA%D0%B0-%D0%9E%D0%B1%D0%BD%D0%BE%D0%B2%D0%B8%D1%82%D1%8C-%D0%B4%D0%B0%D0%BD%D0%BD%D1%8B%D0%B5-%D0%BF%D0%BE%D0%B4%D0%BF%D0%B8%D1%81%D1%87%D0%B8%D0%BA%D0%B0-%D0%9A%D0%94) в документации Sendsay API.

#### Примеры

Создайте объект `Customer`, содержащий как минимум один идентификатор клиента и, опционально, словарь с дополнительными свойствами:

```dart
final customer = Customer(
  ids: {
    'registered': 'jane.doe@example.com',
  },
  properties: {
    'first_name': 'Jane',
    'last_name': 'Doe',
    'age', 32
  },
);
```

Передайте объект клиента в `identifyCustomer()`:

```dart
_plugin.identifyCustomer(customer);
```

Если вы хотите обновить только идентификатор клиента без дополнительных свойств, передайте пустой словарь в `properties`:

```dart
final customer = Customer(
  ids: {
    'registered': 'jane.doe@example.com',
  },
  properties: {},
);
_plugin.identifyCustomer(customer);
```

### Anonymize

Метод `anonymize()` удаляет локальные данные клиента и сбрасывает текущее состояние SDK. Используйте его при выходе пользователя из приложения.

Вызов `anonymize()` приведёт к тому, что SDK:

* удалит push-токен из локального хранилища устройства и профиля клиента в CDP Sendsay;
* очистит локальные репозитории и кэши (кроме уже отслеженных событий);
* отследит новое начало сессии, если включён `automaticSessionTracking`;
* создаст новую запись клиента в Sendsay (генерируется новый `cookie`);
* назначит предыдущий push-токен новой записи клиента;
* отследит новое событие `installation`.

Также метод можно использовать для переключения между проектами CDP Sendsay. SDK будет отслеживать события в новой записи клиента в новом проекте, аналогично первой сессии приложения после установки на новом устройстве.

#### Пример

Вызов `anonymize()`:

```dart
_plugin.anonymize();
```

Переключиться на другой проект:

```dart
final configChange = SendsayConfigurationChange(
  project: SendsayProject(
    projectToken: 'new-project-token',
    authorizationToken: 'new-authorization-token',
  ),
  mapping: {
    EventType.payment: [
      SendsayProject(
        projectToken: 'special-project-for-payments',
        authorizationToken: 'payment-authorization-token',
        baseUrl: 'https://api-payments.some-domain.com',
      ),
    ],
  },
);
_plugin.anonymize(configChange);
```

## Сессии

SDK автоматически отслеживает сессии, генерируя события `session_start` и `session_end`.

Сессия начинается при запуске приложения и завершается при переходе в фоновый режим. По умолчанию тайм-аут сессии составляет 60 секунд и может быть изменён через параметр `sessionTimeout` в [конфигурации SDK](../docs/configuration.md).

### Ручное отслеживание сессий

Чтобы отключить автоматическое отслеживание, установите `automaticSessionTracking = false` в [конфигурации SDK](../docs/configuration.md).

Для ручного отслеживания используйте: 

```dart
trackSessionStart()
trackSessionEnd()
``` 

### Переопределение стандартных свойств

SDK автоматически добавляет свойства (например, `ip`, `city`, `country`) к событиям сессий. Их можно переопределить через параметр `defaultProperties` в конфигурации SDK.

## Push-уведомления

SDK автоматически отслеживает push-токены при [интеграции push-уведомлений](../docs/push-notifications.md#integration) в приложение.

В Android автоматическое отслеживание можно отключить, установив `automaticPushNotification = false` в [конфигурации SDK для Android](../docs/configuration.md#android-specific-configuration-parameters). После этого уведомления нужно отслеживать [вручную](push-notifications.md#ручное-отслеживание-push-уведомлений).

> ❗️
>
> Flutter SDK в настоящее время не поддерживает отключение автоматического отслеживания push-уведомлений на iOS.

> ❗️
>
> На отслеживание push-уведомлений может влиять функция согласия на отслеживание (tracking consent). Подробнее — в [документации по согласию](../docs/tracking-consent).

### Отслеживание токена вручную

Используйте методы `trackPushToken()` (Firebase) или `trackHmsPushToken()` (Huawei) чтобы вручную отслеживать push-токен устройства. Токен привязывается к клиенту, который в данный момент идентифицирован с помощью метода `identifyCustomer()`.

Вызов этих методов приводит к немедленной отправке push-токена в CDP Sendsay, независимо от значения параметра `tokenTrackFrequency`.  Подробнее — в разделе [Конфигурация](../docs/configuration.md).

Кроме того, каждый раз, когда приложение становится активным, SDK автоматически вызывает `verifyPushStatusAndTrackPushToken` и проверяет актуальность push-токена.

#### Аргументы

| Имя                 | Тип    | Описание |
| ---------------------| ------- | ----------- |
| token **(обязательно)** | Строка  | Токен push-уведомления. |

#### Пример

Firebase:

```dart
SendsayPlugin().trackPushToken("value-of-push-token")
```

Huawei:

```dart
SendsayPlugin().trackHmsPushToken("value-of-push-token")
```

> ❗️
>
> Не забывайте вызывать метод [anonymize](#anonymize) при выходе пользователя из приложения. Это необходимо, чтобы удалить push-токен из профиля клиента в CDP Sendsay. Если этого не сделать, один и тот же push-токен может быть привязан к нескольким профилям, что приведёт к дублированию push-уведомлений.

### Отслеживание доставки push-уведомлений вручную

Используйте метод `trackDeliveredPush()` для [ручного отслеживания](../docs/push-notifications.md#отслеживание-доставленных-push-уведомлений) факта доставки push-уведомления.

Метод принимает в качестве аргумента **полезную нагрузку уведомления в формате JSON**.

Пример структуры полезной нагрузки смотрите в разделе [Push notifications](../docs/push-notifications.md#пример-payload).

#### Пример

```dart
SendsayPlugin().trackDeliveredPush(payload)
```

### Отслеживание кликов по push-уведомлениям вручную

Используйте метод `trackClickedPush()` для [ручного отслеживания кликов по push-уведомлениям](../docs/push-notifications.md#отслеживание-нажатых-push-уведомлений).

Метод принимает в качестве аргумента **полезную нагрузку уведомления в формате JSON**.

Пример структуры полезной нагрузки смотрите в разделе [Push notifications](../docs/push-notifications.md#пример-payload).

#### Пример

```dart
SendsayPlugin().trackClickedPush(payload)
```

## Платежи

SDK предоставляет метод `trackPaymentEvent()`, который позволяет отслеживать платежи за товары или услуги в мобильном приложении.

### Отслеживание событий платежей

Для отслеживания события платежа используйте метод `trackPaymentEvent()`.

#### Аргументы

| Имя | Тип | Описание |
| --- | --- | --- |
| purchasedItem **(обязательно)** | PurchasedItem | Объект с данными о платеже. |
| timestamp | DateTime? | Временная метка события (Unix, в секундах). По умолчанию используется текущее время. |

#### Примеры

Создайте объект `PurchasedItem` с основной информацией о покупке:

```dart
final item = PurchasedItem(
    value = 12.34,
    currency = "EUR",
    paymentSystem = "Virtual",
    productId = "handbag",
    productTitle = "Awesome leather handbag"
)
```

Передайте его в `trackPaymentEvent`:

```dart
SendsayPlugin().trackPaymentEvent(item)
```

## Свойства по умолчанию

Вы можете [настроить](configuration.md) **свойства по умолчанию**, которые будут автоматически добавляться ко всем отслеживаемым событиям. Если событие содержит свойство с тем же ключом, значение по умолчанию будет **перезаписано**.

```dart
final config = SendsayConfiguration(
    projectToken: 'YOUR_PROJECT_TOKEN',
    authorizationToken: 'YOUR_API_KEY',
    baseUrl: 'YOUR_API_BASE_URL', 
    defaultProperties: const {
    'thisIsADefaultStringProperty': 'This is a default string value',
    'thisIsADefaultIntProperty': 1
    },
);
_plugin.configure(configuration);
```

После инициализации SDK вы можете изменить свойства по умолчанию с помощью метода `SendsayPlugin.setDefaultProperties()`.
